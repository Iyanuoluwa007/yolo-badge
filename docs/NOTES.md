# Notes

## Branch naming
Use `type/short-description`, e.g. `docs/contributing`, `fix/null-guard`.

## Commit messages
Use a conventional prefix (`docs:`, `fix:`, `chore:`, `feat:`) followed by a
short imperative summary.

## Merging
Prefer merge commits so the pull request history stays visible.
